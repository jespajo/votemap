Add zoom control to mobile
Add rotation to mobile

Add electoral district shapefiles to database
Don't let Voronoi polygons cross electoral district boundaries

Add JSON module to src/
Output canvas markup in JSON format and draw it on screen

Add XML vote data/election info to database

Create materialised views for efficient access to the XML data
    tables:
        booths
        districts
        candidates
        votes (first preferences)
        votes (two-party-preferred)

Figure out URL path structure
    bounds
    zoom level
    layer identifier
Figure out how many zoom levels you want to have

Turn main() into a CGI script
